apiVersion: v1
kind: pod
metadata:
  name: nginx
  label:
    app.kuberenets.io/name: proxy
spec:
  containers:
  - name: nginx
    image: nginx:stable
    ports:
      - containerport: 80
        name: http-web-svc

